algoritmo "SomaParametrosValor"

var
X, Y: inteiro

procedimento soma(A, B: inteiro)
inicio
    escreval("Recebi o valor de ", A)
    escreval("Recebi o valor de ", B)
    escreval("A soma entre os dois é: ", A+B)
fimProcedimento

inicio
X := 5
Y := 10

soma(X, Y)
fimalgoritmo