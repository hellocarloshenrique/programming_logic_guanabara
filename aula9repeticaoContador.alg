algoritmo "Contador"

var
    contador: inteiro

inicio
    contador <- 20
    Enquanto (contador >= 0) faca
    escreval("numero ", contador)
    contador <- (contador - 1)
    FimEnquanto
    escreval("terminei de escrever.")

fimalgoritmo