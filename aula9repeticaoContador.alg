algoritmo "Contador"

var
    contador: inteiro

inicio
    contador <- 0
    Enquanto (contador <= 10) faca
    escreval("numero ", contador)
    contador <- (contador + 1)
    FimEnquanto

fimalgoritmo